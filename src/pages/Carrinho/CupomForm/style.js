import styled from "@emotion/styled";

const FormCupom = styled((props) => <form {...props} />)({
  display: "flex",
  alignContent: "center",
  flexWrap: "wrap",
  gap: "1rem",
});

export { FormCupom };
